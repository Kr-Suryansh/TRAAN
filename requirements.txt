# ── Core Framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.6

# ── Database ────────────────────────────────────────────────────────────────────
# asyncpg 0.31.0+ ships pre-built wheels for Python 3.14 on Windows.
# Earlier versions required MSVC + Cython compilation.
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.31.0
geoalchemy2>=0.15.2
alembic>=1.13.3

# ── Configuration ───────────────────────────────────────────────────────────────
# pydantic 2.10+ (pydantic-core 2.27+) ships pre-built wheels for Python 3.14.
# Earlier versions required Rust/MSVC compilation via maturin.
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-multipart>=0.0.12

# ── Auth (wired in Phase 2 — installed now to avoid rebuild later) ──────────────
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt<4.0.0

# ── HTTP client for internal calls & tests ──────────────────────────────────────
httpx>=0.27.2

# ── Testing ─────────────────────────────────────────────────────────────────────
pytest>=8.3.3
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
